# Keeps the SHA-pinned actions in .github/workflows current. Grouped, so a week
# with several bumps is one pull request rather than one per action.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      github-actions:
        patterns: ['*']
